package com.sms.core.enums;

public enum TaxRegime {
    /** Old regime — allows 80C, HRA, LTA, etc. Standard deduction ₹50,000. */
    OLD,
    /** New regime (default from FY 2024-25) — standard deduction ₹75,000, no 80C/HRA. */
    NEW
}
